<UserControl x:Class="Cartogram.MapTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="MapUserControl">
    <Grid Background="#59000000">
        <Grid Width="{Binding ActualWidth, ElementName=MapUserControl}">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="80*"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <Label x:Name="LabelTitle" Grid.Row="0" Foreground="White" Content="Fleeting Dried Lake Map of Ice" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
